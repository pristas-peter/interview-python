[project]
name = "interview-python"
version = "0.0.0"
description = "Coding exercise — loan pre-screen service."
requires-python = ">=3.11"
dependencies = [
    # Add the dependencies you need (web framework, openai SDK, etc.).
]

[project.optional-dependencies]
dev = [
    # Add test/lint tools you want (pytest, ruff, mypy, ...).
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
